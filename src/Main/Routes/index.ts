import { Router } from "express";

// Importe e registre as rotas de cada modulo aqui:
// import { usersRouter } from "./_module-name_/Routes";

export const router = Router();

// router.use("/users", usersRouter);
